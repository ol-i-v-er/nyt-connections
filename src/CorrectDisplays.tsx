import styles from "./Connections.module.css"

export function CorrectDisplay() {
    return (
        <div className={`${styles.correctDisplay}`}>
            <div>HIII BUYDDYD</div>
            <div style={{fontWeight: "normal", fontSize: "1.5rem"}}>word, word,  word, word</div>
        </div>
    )
}